# Penguin Port Backend 기여 가이드

## 1. 반드시 지켜야 할 규칙

- 커밋 메시지는 정해진 컨벤션에 따라 간결하고 직관적으로 작성합니다.
- 이미 공유된 원격 브랜치의 히스토리를 임의로 수정하지 않습니다. `force push`를 사용하지 않습니다.
- PR을 `main` 브랜치에 머지하기 전, 최소 1명 이상의 팀원에게 검토와 Approve를 받습니다.
- 리뷰가 완료된 PR은 작성자가 직접 충돌을 해결하고 머지합니다.
- 각 브랜치의 수명은 최대한 짧게 유지합니다.

브랜치의 수명이 길어질수록 `main`과의 격차가 벌어지고 Rebase 과정에서 충돌할 가능성이
커집니다. 기능은 가능한 한 작은 단위로 나누어 빠르게 머지합니다.

## 2. 워크플로우

모든 작업은 GitHub Issue 생성으로 시작합니다.

1. GitHub Issue를 생성합니다.
   - 형식: `[FEATURE/FIX] 이슈 내용`
   - 예시: `[FEATURE] 구글 소셜 로그인 구현`
   - 예시: `[FIX] 게시판 API 응답 형식 수정`
2. 최신 `main`에서 새 브랜치를 생성합니다.
   - 형식: `feature/#{이슈번호}-작업내용`
   - 예시: `feature/#12-google-login`
   - 예시: `fix/#13-board-api-response-format`
3. 분기한 브랜치에서 작업합니다.
4. 구현 단위별로 커밋 메시지 컨벤션에 맞는 커밋을 남깁니다.
5. 필요한 경우 커밋을 Squash하여 히스토리를 간결하게 정리합니다.
6. `git fetch origin`으로 최신 원격 코드를 가져옵니다.
7. 최신 `main` 위로 작업 브랜치를 Rebase하고 충돌을 해결합니다.
8. Rebase가 완료된 브랜치를 원격 저장소에 푸시합니다.
9. Pull Request를 생성합니다.
10. 리뷰어 피드백을 반영하고 최종 Approve를 받습니다.
11. 작성자가 직접 머지한 뒤 원격·로컬 작업 브랜치를 삭제합니다.

```text
main
  └── feature/#12-google-login
  └── fix/#13-board-api-response-format
```

## 3. 커밋 메시지 컨벤션

유다시티 컨벤션을 따르되 메시지의 핵심 내용은 한글로 작성합니다.

### 기본 형식

```text
feat: 구글 소셜 로그인 api 구현

변경 이유, 변경 내용, 영향을 받는 부분을 필요한 경우 본문에 작성합니다.
```

### 타입

| 타입 | 설명 | 예시 |
| --- | --- | --- |
| `feat` | 새로운 기능 추가 | `feat: 매칭 룸 생성 api 추가` |
| `fix` | 버그 수정 | `fix: 로그인 토큰 만료 에러 수정` |
| `docs` | README, Wiki 등 문서 수정 | `docs: api 명세서 링크 업데이트` |
| `refactor` | 기능 변경 없는 코드 개선 | `refactor: 유저 서비스 로직 중복 제거` |
| `test` | 테스트 코드 추가·수정 | `test: 인증 로직 단위 테스트 추가` |
| `chore` | 빌드·패키지 등 설정 변경 | `chore: 의존성 추가` |
| `comment` | 주석 추가·변경 | `comment: 정렬 알고리즘 설명 추가` |
| `remove` | 파일 또는 폴더 삭제 | `remove: 미사용 컴포넌트 삭제` |
| `rename` | 파일 또는 폴더 이름·위치 변경 | `rename: dto 클래스 위치 변경` |

### 작성 규칙

- 제목은 반드시 `<type>: <제목>` 형식을 사용합니다.
- 타입 뒤에 콜론과 공백 한 칸을 넣습니다.
- 제목은 명령형으로 간결하고 직관적으로 작성합니다.
- 상세 설명이 필요하면 제목 아래 한 줄을 띄우고 본문을 작성합니다.
- 본문에는 변경 이유, 변경 내용, 영향을 받는 부분을 적습니다.
